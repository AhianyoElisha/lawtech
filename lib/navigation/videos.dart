import 'package:flutter/material.dart';

class ScrollVideosPage extends StatelessWidget {
  const ScrollVideosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Infinite Scroll Videos Page"),
    );
  }
}
