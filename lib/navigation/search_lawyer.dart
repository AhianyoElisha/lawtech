import 'package:flutter/material.dart';

class SearchLawyerPage extends StatelessWidget {
  const SearchLawyerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Book Lawyer Page"),
    );
  }
}
